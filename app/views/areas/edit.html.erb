<h1>Alterando Area</h1>

<% form_for(@area) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => "form",  :locals => { :f => f} %>

  <p>
    <%= f.submit 'Salvar' %>
  </p>
<% end %>

<%= link_to 'Exibir', @area %>
<%= link_to 'Voltar', areas_path %>
