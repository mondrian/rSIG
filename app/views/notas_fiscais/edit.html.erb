<h1>Edicao de Nota Fiscal</h1>

<% form_for(@nota_fiscal) do |f| %>
  <%= f.error_messages %>

  <%= render :partial => 'form', :locals => {:f => f} %>
  <p>
    <%= f.submit 'Atualizar' %>
  </p>
<% end %>

<%= link_to 'Exibir', @nota_fiscal %> |
<%= link_to 'Voltar', notas_fiscais_path %>r
